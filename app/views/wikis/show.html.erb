<h1><%= markdown @wiki.subject %></h1>

<%= link_to "Edit", edit_wiki_path(@wiki), class: 'btn btn-success' %>

<%= render partial: 'form', locals: { wiki: @wiki, user: @user} %>


<%= render partial: 'join', locals: { user: @user } %>
  </div>

<div class="control-group span13 columns4 checkbox-group">
      <div class="controls">
  <%= hidden_field_tag "wiki[user_ids][]", nil%>
            <% User.all.each do |user| %>
                  <label class="checkbox">
                      <%= check_box_tag "wiki[user_ids][]", user.id, @wiki.user_ids.include?(user.id), id: dom_id(user) %>
                      <%= user.email %>
                  </label>
            <% end %>
      </div>
</div>



<%= form_tag("/wikis/#{@wiki.id}/add_collaborator", method: "post") do %>
  <%= label_tag(:user_id, "User id:") %>
  <%= text_field_tag(:user_id) %>
  <%= submit_tag("Add collaborator") %>
<% end %>

     